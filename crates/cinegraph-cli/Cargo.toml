[package]
name = "cinegraph"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
anyhow.workspace = true
clap.workspace = true
cinegraph-core = { path = "../cinegraph-core" }
cinegraph-db = { path = "../cinegraph-db" }
cinegraph-fetch = { path = "../cinegraph-fetch" }
cinegraph-imdb = { path = "../cinegraph-imdb" }
cinegraph-search = { path = "../cinegraph-search" }
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true

[dev-dependencies]
tempfile.workspace = true
